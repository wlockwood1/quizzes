<nav class="navbar navbar-default navbar-fixed-top navbar-inverse">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Kwiz</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li class="search"><%= render 'shared/search' %></li>
        <% if user_signed_in? %>
            <li><%= link_to 'Play Random Quiz', quiz_path(Quiz.all.shuffle) %></li>
            <li class="new_category"><%= link_to 'New Category', new_category_path %></li>
            <li class="new_quiz"><%= link_to 'New Quiz', new_quiz_path %></li>
            <li role="presentation" class="dropdown">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
                <%= current_user.username %><span class="caret"></span>
              </a>
              <ul class="dropdown-menu">
                <!-- Need to fix. Stops working after game plays unless you refresh page-->
                    <li><%= link_to "Profile Page", user_path(current_user) %></li>
                    <li><%= link_to "Account Settings", edit_user_registration_path %></li>
                    <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
              </ul>
            </li>
            </li>
        <% else %>
            <li role="presentation" class="dropdown">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
                Log In<span class="caret"></span>
              </a>
              <ul class="dropdown-menu">
                <!-- Need to fix. Stops working after game plays unless you refresh page-->
                <li><%= render 'shared/login' %>
              </ul>
            <li role="presentation" class="dropdown">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
                Sign Up!<span class="caret"></span>
              </a>
              <ul class="dropdown-menu">
                <!-- Need to fix. Stops working after game plays unless you refresh page-->
                <li><%= render 'shared/signup' %>
              </ul>
        <% end %>
        <li role="presentation" class="dropdown">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
                Categories<span class="caret"></span>
              </a>
              <ul class="dropdown-menu">
                <!-- Need to fix. Stops working after game plays unless you refresh page-->
                <% Category.all.each do |category| %>
                    <li class="category"><%= link_to category.name, category %></li>
                <% end %>
              </ul>
            </li>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>